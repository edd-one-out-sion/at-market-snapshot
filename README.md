# aT Wholesale Market Collector

한국 공공데이터포털의 aT 도매시장 경락 OpenAPI를 정식 인증키로 호출해 수집하고, 비공개 데이터베이스에 적재하는 개인 프로젝트용 수집기입니다.

이 저장소는 수집 코드만 공개합니다. 수집 결과와 백업 파일은 포함하지 않으며 비공개 저장소에만 보관합니다.

## Scheduled operation

GitHub Actions는 KST 02:17, 04:07, 05:17, 07:07, 12:07, 21:07에 실행됩니다.
05:17은 당일 realtime을 필수로 검사하고 전일 origin 미충족은 경고만 남깁니다.
07:07은 당일 realtime과 전일 origin을 모두 필수로 검사합니다. 수동 실행도 KST
04:30~11:59에 시작하면 07:07과 같은 전체 기준선을 검사합니다. 무장 여부는
workflow 표현식이 아니라 GitHub 이벤트 payload와 KST 시각을 loader가 직접
읽어 결정합니다.

기준선은 고정 행수를 사용하지 않습니다. 실행 시점의 `collect=true` 시장 수를 읽고,
당일은 모든 활성 시장의 `at-realtime`, 전일은 모든 활성 시장의 `at-origin`이
각각 1행 이상 존재해야 통과합니다. 휴장 면제는 일요일과 repo 변수
`PLANNED_CLOSURE`로 등록한 날짜·시장만 허용합니다. 값은 쉼표로 구분하고
`YYYY-MM-DD`는 전 시장, `YYYY-MM-DD:whsal_cd`는 한 시장만 면제합니다.
빈 토큰과 값 `0`은 무시하며, 형식 오류나 비활성 시장은 수집을 중단하지 않고
step summary에 경고로 남깁니다.
API 성공+0행은 공급 장애나 확정본 미발행과 구분할 수 없으므로 참고 경고만 남기고
절대 휴장으로 간주하지 않습니다.

세 시장을 모두 엄격하게 검사하는 근거는 과거 07:07 예약 실행 로그입니다.
정상 완료된 영업일 2026-08-10, 2026-08-11, 2026-08-13에 가락·남촌·삼산 모두
당일 realtime 적재가 확인됐습니다. 시장별 행수는 각각
`4916/501/189`, `4548/596/235`, `4582/543/272`였습니다.

일요일 규칙은 2026-08-03 이후 첫 일요일인 2026-08-09 정산일을 DB에서
실조회해 가락 `0`, 남촌 `0`, 삼산 `0`임을 확인했습니다. 전일 origin 확정본도
05:17 이전에 존재했습니다. 2026-08-12 03:13 KST에는 2026-08-11 확정본이
`4544/520/369`, 2026-08-13 05:12 KST에는 2026-08-12 확정본이
`4704/485/412`행으로 세 시장 모두 적재됐습니다.

## Failure gate

모든 예약·수동 실행에서 다음 조건을 모두 만족하는 경우에만 job을 성공+warning으로
끝냅니다.

- 연속 실패 차단기가 발동했고 시도한 묶음이 모두 timeout
- 성공 묶음, 비-timeout 실패, 수집 행이 모두 0
- 적재 준비·건너뜀·보류·삭제·신규 적재가 모두 0
- 불완전 후보만 기록되어 기존 DB 데이터가 그대로 유지됨
- 비공개 백업 성공

부분 수집, 수집 후 0적재, 시간 예산 종료, 비-timeout 오류, 적재/RPC 실패,
백업 결과 누락·실패, 아침 기준선 미달은 계속 job 실패로 처리합니다.
아침 창 밖 수동 실행의 초록은 일반 Gate 통과만 뜻하며 아침 기준선 충족을
보장하지 않습니다.

## GitHub Actions Variables

- `PLANNED_CLOSURE`

## GitHub Actions Secrets

- DATA_GO_KR_API_KEY_ENC
- SUPABASE_URL
- SUPABASE_SERVICE_KEY
- BACKUP_PUSH_TOKEN
- BACKUP_REPO

## License

Source code is licensed under the MIT License. See LICENSE.
